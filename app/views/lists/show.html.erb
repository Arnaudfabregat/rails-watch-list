<%= @list.name %>

<% @list.bookmarks.each do |bookmark| %>
  <%= render "asset/movie_card", movie: bookmark.movie, bookmark: bookmark%>
<% end %>

<%= link_to "Add a new Movie", new_list_bookmark_path(@list) %>
