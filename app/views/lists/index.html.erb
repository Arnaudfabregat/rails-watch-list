
<%= render "asset/banner" %>

<div class="container">
<div class="row">
  <% @lists.each do |list| %>
    <div class="col-sm">
    <p>
      <%= render "asset/list_card", list: list%>
    </p>
    </div>
  <% end %>
</div>
</div>
<%= link_to "Add a new List", new_list_path %>
